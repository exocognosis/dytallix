# Polkadot Metadata File
# This is a placeholder for the actual Polkadot runtime metadata
# In production, this would be generated by running:
# subxt metadata --url wss://rpc.polkadot.io > polkadot_metadata.scale

# For now, we'll use the subxt macro with inline types to avoid requiring the metadata file
