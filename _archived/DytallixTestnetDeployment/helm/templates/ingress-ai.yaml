{{- if and .Values.global.ingress.enabled .Values.ai.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dytallix-ai
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.global.ingress.clusterIssuer | quote }}
    {{- with .Values.global.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.ai.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.global.ingress.className | quote }}
  tls:
    - hosts:
        - {{ .Values.ai.ingress.host | quote }}
      secretName: {{ .Values.ai.ingress.tlsSecret | quote }}
  rules:
    - host: {{ .Values.ai.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ai.service.name | quote }}
                port:
                  number: {{ .Values.ai.service.port }}
{{- end }}

