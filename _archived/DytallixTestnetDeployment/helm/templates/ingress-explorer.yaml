{{- if and .Values.global.ingress.enabled .Values.explorer.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dytallix-explorer
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.global.ingress.clusterIssuer | quote }}
    {{- with .Values.global.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.explorer.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.global.ingress.className | quote }}
  tls:
    - hosts:
        - {{ .Values.explorer.ingress.host | quote }}
      secretName: {{ .Values.explorer.ingress.tlsSecret | quote }}
  rules:
    - host: {{ .Values.explorer.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.explorer.service.name | quote }}
                port:
                  number: {{ .Values.explorer.service.port }}
{{- end }}

