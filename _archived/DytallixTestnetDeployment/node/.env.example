# Node (.env.example)
# Applies if the node image reads envs for ports/peers; otherwise keep for documentation.

P2P_PORT=26656
RPC_PORT=3030
GRPC_PORT=9090
API_PORT=1317
MONIKER=dytallix-testnet-node
SEEDS=
PERSISTENT_PEERS=
PRUNING=default
MIN_GAS_PRICE=0.025uDRT
