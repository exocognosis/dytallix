# Counter Example

Minimal stateful contract example for Dytallix.

It demonstrates:

- instantiate, execute, and query message separation
- owner-gated admin behavior
- deterministic state changes with unit tests

Run:

```bash
cargo test --manifest-path examples/counter/Cargo.toml
```

