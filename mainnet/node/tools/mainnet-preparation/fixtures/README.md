# Synthetic local binding fixtures

These four JSON files are test inputs. They do not contain approved production records.

The native and application shapes derive from the sealed genesis preparation fixture. Four synthetic accounts each bond 25 uDGT to one synthetic operator. The team fixture retains its explicit short vesting schedule. The total mint is a local arithmetic fixture, not a production issuance decision.

The public peer keys and loopback addresses derive from retained local transport evidence. References beginning with `fixture-` are test identifiers. Null custody and approval fields remain null. The supported binding check does not replace the production record intake checker.
