[package]
name = "dytallix-gas"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
dytallix-protocol-types = { path = "../protocol-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
dytallix-storage = { path = "../storage" }
