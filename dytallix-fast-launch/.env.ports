# Dytallix Fixed Port Configuration
# All services use these exact ports - no more random port assignment!

# Frontend (React/Vite)
VITE_PORT=3000
VITE_HOST=0.0.0.0

# API URLs for frontend
VITE_API_URL=http://localhost:8787
VITE_QUANTUMVAULT_API_URL=http://localhost:3031
VITE_BLOCKCHAIN_URL=http://localhost:3003
VITE_RPC_HTTP_URL=http://localhost:3030
VITE_WEBSOCKET_URL=ws://localhost:9000

# Blockchain Core (Rust)
BLOCKCHAIN_PORT=3003
BLOCKCHAIN_HOST=0.0.0.0
RPC_PORT=3030
P2P_PORT=30303

# QuantumVault API (Node.js)
QUANTUMVAULT_PORT=3031
QUANTUMVAULT_HOST=0.0.0.0

# Advanced API (Node.js)
ADVANCED_API_PORT=8787
ADVANCED_API_HOST=0.0.0.0

# Backend API
BACKEND_PORT=8080
BACKEND_HOST=0.0.0.0

# WebSocket Server
WEBSOCKET_PORT=9000
WEBSOCKET_HOST=0.0.0.0

# Database (if needed)
DATABASE_PORT=5432

# Redis (if needed)
REDIS_PORT=6379

# Production URLs (for deployment)
PROD_FRONTEND_URL=https://dytallix.com
PROD_API_URL=https://api.dytallix.com
PROD_QUANTUMVAULT_URL=https://quantumvault.dytallix.com
PROD_BLOCKCHAIN_URL=https://blockchain.dytallix.com
