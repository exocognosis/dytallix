{
  "name": "dytallix-faucet",
  "version": "0.1.0",
  "description": "Dytallix DGT/DRT Token Faucet for Testnet",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build": "echo 'No build step required for Node.js'",
    "test": "node --test test/*.test.js",
    "lint": "eslint src/ --fix",
    "health": "curl -f http://localhost:8787/health || exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0",
    "express-rate-limit": "^6.10.0",
    "express-slow-down": "^1.6.0",
    "node-cron": "^3.0.2",
    "axios": "^1.5.0",
    "compression": "^1.7.4",
    "winston": "^3.10.0",
    "joi": "^17.10.0",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "dytallix",
    "faucet",
    "dgt",
    "drt",
    "testnet",
    "blockchain"
  ],
  "author": "Dytallix Team",
  "license": "Apache-2.0"
}