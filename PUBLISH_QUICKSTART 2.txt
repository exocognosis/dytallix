╔════════════════════════════════════════════════════════════════╗
║           📦 DYTALLIX SDK PUBLISHING QUICK START              ║
╚════════════════════════════════════════════════════════════════╝

🎯 GOAL: Publish @dytallix/sdk to NPM so developers can install it

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 THE SIMPLEST PATH (5 MINUTES):

1. CREATE NPM ACCOUNT
   → Go to: https://www.npmjs.com/signup
   → Pick a username and verify email

2. LOGIN FROM TERMINAL
   → cd ~/dytallix/dytallix-fast-launch/sdk
   → npm login
   → Enter credentials

3. RUN THE WIZARD
   → ./publish-interactive.sh
   → Follow the prompts
   → Done! 🎉

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🛠️ MANUAL STEPS (IF YOU PREFER):

# 1. Build the SDK
cd ~/dytallix/dytallix-fast-launch/sdk
npm install
npm run build

# 2. Verify everything
./verify-publish.sh

# 3. Publish
npm publish --access public

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT NOTES:

• Package name is "@dytallix/sdk" (scoped package)
• MUST use --access public (scoped packages are private by default)
• This is a ONE-TIME action (you can't unpublish after 24 hours)
• Anyone can install it: npm install @dytallix/sdk

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎭 ALTERNATIVE: Use Your Personal Scope

If you don't want to create @dytallix organization:

1. Edit package.json
2. Change: "@dytallix/sdk" 
   → To: "@YOUR_USERNAME/dytallix-sdk"
3. Then publish: npm publish --access public

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 FILES CREATED FOR YOU:

✓ SDK_PUBLISHING_GUIDE.md - Complete guide (in workspace root)
✓ publish-checklist.md - Pre-publish checklist
✓ publish-interactive.sh - Guided wizard 🧙
✓ verify-publish.sh - Pre-publish verification

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔮 AFTER PUBLISHING:

✓ Visit: https://www.npmjs.com/package/@dytallix/sdk
✓ Test install: npm install @dytallix/sdk
✓ Create git tag: git tag v0.1.0 && git push --tags
✓ Announce on social media

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 TROUBLESHOOTING:

Problem: "You do not have permission to publish @dytallix/sdk"
Solution: Either create @dytallix org on NPM, or use your own scope

Problem: "Package name already taken"
Solution: Choose a different name or add scope

Problem: "Not logged in"
Solution: Run: npm login

Problem: Build fails
Solution: Check if node_modules exists, run npm install

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 READY TO PUBLISH?

Option 1 (Easiest): ./publish-interactive.sh
Option 2 (Manual):  npm publish --access public

Need help? See: SDK_PUBLISHING_GUIDE.md

╚════════════════════════════════════════════════════════════════╝
